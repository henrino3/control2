<p id="notice"><%= notice %></p>

<h1>Listing Citizens</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Phone num</th>
      <th>Email</th>
      <th>Address</th>
      <th>National Id</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @citizens.each do |citizen| %>
      <tr>
        <td><%= citizen.name %></td>
        <td><%= citizen.phone_num %></td>
        <td><%= citizen.email %></td>
        <td><%= citizen.address %></td>
        <td><%= citizen.national_id %></td>
        <td><%= link_to 'Show', citizen %></td>
        <td><%= link_to 'Edit', edit_citizen_path(citizen) %></td>
        <td><%= link_to 'Destroy', citizen, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Citizen', new_citizen_path %>
<%= link_to 'Sign out', destroy_admin_session_path , method: :delete, data: { confirm: 'Are you sure?' } %> !
 <table>
 <thead>
    <tr>
      <th>Name</th>
      <th>Bank type</th>
      <th>Registration no</th>
      <th>Founded data</th>
      <th>Main service</th>
      <th>Bank email</th>
      <th>Bank website</th>
      <th>Bank logo url</th>
      <th>Token</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @banks.each do |bank| %>
      <tr>
        <td><%= bank.name %></td>
        <td><%= bank.bank_type %></td>
        <td><%= bank.registration_no %></td>
        <td><%= bank.founded_data %></td>
        <td><%= bank.main_service %></td>
        <td><%= bank.email %></td>
        <td><%= bank.bank_website %></td>
        <td><%= bank.bank_logo_url %></td>
        <td><%= bank.token %></td>
        <td><%= link_to 'Show', bank %></td>
        <td><%= link_to 'Edit', edit_bank_path(bank) %></td>
        <td><%= link_to 'Destroy', bank, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
  </table>

  <h1>dashboard content:</h1>
  <h4>Number of banks: <%= @numBank %></h4>
  <h4>Number of citizens: <%= @numCitizen %></h4>